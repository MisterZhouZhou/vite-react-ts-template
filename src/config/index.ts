// 接口api地址
export const URL = {
	musicUrl: 'https://xxx.com'
}

// 全局常量
export const CT = {
	timeout: 9000
}

// serve delete
// proxy 前缀
export const proxyApi = '/api'
export const apiAddress = 'http:/www.baidu.com'

export default {}
